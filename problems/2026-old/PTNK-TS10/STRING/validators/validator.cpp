#include <bits/stdc++.h>
#include "testlib.h"
using namespace std;

signed main(int argc, char* argv[]){
    registerValidation(argc, argv);
    
    string S = inf.readToken("[a-z]{1, 1000000}", "S");
    inf.readEoln();
    inf.readEof();

    return 0;
}